{
  "name": "kanban",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --env=jsdom",
    "test:watch": "jest --env=jsdom --watchAll",
    "refactor": "jest && npm run bundle && git commit --amend --no-edit -a || git reset --hard",
    "typecheck": "tsc --noEmit",
    "bundle": "node bundle.js",
    "bundle:watch": "nodemon --watch src --ext js,ts bundle.js",
    "test:e2e": "playwright test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/jest": "^30.0.0",
    "@types/node": "^25.5.0",
    "browserify": "^17.0.1",
    "jest": "^24.8.0",
    "node-notifier": "^10.0.1",
    "nodemon": "^3.1.14",
    "open": "^11.0.0",
    "serve": "^14.2.6",
    "tsify": "^5.0.4",
    "typescript": "^5.9.3"
  },
  "jest": {
    "testPathIgnorePatterns": [
      "/node_modules/",
      "/e2e/"
    ]
  },
  "dependencies": {
    "chart.js": "^4.5.1",
    "pubsub-js": "^1.9.5"
  }
}
