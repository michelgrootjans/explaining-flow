{
  "name": "kanban",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "autotest": "jest --watchAll",
    "refactor": "jest && npm run bundle && git commit --amend -am 'refactor' || git reset --hard",
    "bundle": "browserify src/*.js > dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^16.2.3",
    "jest": "^24.8.0"
  },
  "dependencies": {
    "jquery": "^3.4.1",
    "pubsub-js": "^1.7.0"
  }
}
